# comp0258-evogym-project

## Setup

Clone with submodules: 
`git clone --recursive [URL]`
(submodule adds EvoGym repository)

Install Git LFS: 
`git lfs install`
(helps with large file storage)

Run Setup: 
`bash scripts/setup_all.sh`
(This will build the physics engine (might take a few minutes).)